describe('Data Types', function () {
    it('should must declare', function () {
        let name: string = 'Syauqi Futtaqi';
        let balance: number = 1000000;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

        // name = 1;
        // balance = '1000000';
        // isVip = 'true';
    });
});